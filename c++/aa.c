/*poohcid rugrawi M.4/2 No.9*/
#include<stdio.h>
main()
{
	int N,NUMBER;
      float sum=1,avg=0;
      for(N=1;N<=5;N++);
	
      	printf("Enter number %d : ",N);
      	scanf("%d",&NUMBER);
      	sum=NUMBER;
	  }
	  avg=sum/5;
	  printf("\naverage is %.2f",avg);
	  getch();
}
